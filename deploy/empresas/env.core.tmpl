# Núcleo multiempresa — generado por scripts/nueva_empresa.sh el __FECHA__
# Contraseña root del MySQL compartido (solo la usan los scripts; nunca las empresas).
MYSQL_ROOT_PASSWORD=__ROOT_PASSWORD__
# IP pública del servidor (para hosts permitidos y la dirección <slug>.<ip>.nip.io).
IP_PUBLICA=82.223.44.29
# Llave de OpenRouter que se copia a cada empresa nueva (vacía = IA apagada hasta
# que se capture una llave propia en su .env). Decidir por cliente quién la paga.
OPENROUTER_API_KEY_COMPARTIDA=
