/* ═══════════════════════════════════════════════════════════════════════
 * crm_features_misc.js — CÓDIGO NUEVO (desde 2026-06-04)
 *
 * Cajón para features nuevas que NO encajan claramente en otro
 * archivo *_v2.js. Ejemplos:
 *   - Atajos de teclado globales nuevos
 *   - Polyfills o utilidades transversales
 *   - Mini-features (ej. botón nuevo en el topbar que no es de un módulo)
 *
 * Si esto crece y se vuelve un grupo coherente de funciones, partir a
 * un archivo *_v2.js dedicado (ej. crm_atajos_v2.js).
 *
 * NO usar este archivo para:
 *   - Modificar features existentes del legacy → ir al *_v2.js correcto
 *   - Helpers compartidos → ir a helpers_shared.js cuando se cree
 *
 * Convenciones obligatorias:
 *   - IIFE con 'use strict'
 *   - Idempotente
 *   - Comentar por qué cada feature está aquí y no en otro lado
 *
 * Para activar: agregar `<script src=".../crm_features_misc.js">` en
 * `_scripts_main.html` después de crm_main.js.
 * ═══════════════════════════════════════════════════════════════════════ */

(function () {
    'use strict';
    // (vacío por ahora — el primer feature suelto aterriza aquí)
})();
