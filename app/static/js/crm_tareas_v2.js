/* ═══════════════════════════════════════════════════════════════════════
 * crm_tareas_v2.js — CÓDIGO NUEVO (desde 2026-06-04)
 *
 * Aquí va TODA modificación o nueva feature relacionada con tareas del
 * CRM (cockpit de tareas, subtareas, comentarios, timer, menciones,
 * historial). NO modificar las secciones de tareas en crm_main.js
 * (líneas ~6700-10825 aprox) — están marcadas como LEGACY por Boy
 * Scout Rule.
 *
 * Cuándo escribir aquí:
 *   - Nueva feature de tareas (drag entre estados, nuevos filtros, etc.)
 *   - Cambios al sistema de comentarios o menciones
 *   - Mejoras visuales del cockpit
 *   - Nuevas integraciones (ej. exportar a calendario externo)
 *
 * Cuándo escribir en crm_main.js (legacy):
 *   - Solo bug crítico de producción
 *
 * Convenciones obligatorias:
 *   - IIFE: (function () { 'use strict'; ... })();
 *   - Idempotente para Turbo (cuando se active): guard con window._wired
 *   - Listeners delegation en `document` o `tareasSection` cuando aplique
 *   - Sin console.log de debug
 *
 * Para activar: agregar `<script src=".../crm_tareas_v2.js">` en
 * `_scripts_main.html` después de crm_main.js.
 * ═══════════════════════════════════════════════════════════════════════ */

(function () {
    'use strict';
    // (vacío por ahora — el primer feature de tareas aterriza aquí)
})();
