/* ═══════════════════════════════════════════════════════════════════════
 * crm_clientes_v2.js — CÓDIGO NUEVO (desde 2026-06-04)
 *
 * Aquí va TODA modificación o nueva feature relacionada con el dashboard
 * de clientes / prospección / proyectos vista (ckAbrirDetalle, ckRender*,
 * gráficas drill-down). NO modificar las secciones de "ck*" en crm_main.js
 * (líneas ~3900-4170 aprox) — están marcadas como LEGACY por Boy Scout.
 *
 * Cuándo escribir aquí:
 *   - Nuevas gráficas en el dashboard de clientes
 *   - Cambios en el drill-down de prospectos vs oportunidades
 *   - Nuevos KPIs por cliente
 *   - Mejoras al desglose de facturación / cobrado
 *
 * Convenciones obligatorias:
 *   - IIFE: (function () { 'use strict'; ... })();
 *   - Idempotente, sin debug logs
 *   - Reusar window.CRMHelpers (cuando se cree) en lugar de redefinir
 *     escapeHtml, fmtMoney, etc.
 *
 * Para activar: agregar `<script src=".../crm_clientes_v2.js">` en
 * `_scripts_main.html` después de crm_main.js.
 * ═══════════════════════════════════════════════════════════════════════ */

(function () {
    'use strict';
    // (vacío por ahora — el primer feature del dashboard de clientes aterriza aquí)
})();
